<section class="me-timeline-section timeline-fb">
	<header>
		<i class="icon-facebook"></i>
	</header>
	{{#if is_desktop}}
		<span class="timeline-curtime">{{timeHourMin this.created_time}}</span>
	{{/if}}
	<aside class="fb-feed">
	{{!-- {{#foreach this.elements}} --}}
		{{> timeline/fb/box }}
	{{!-- {{/foreach}} --}}
	</aside>  <!-- /fb-feed-->
</section> <!-- /timeline-fb-->